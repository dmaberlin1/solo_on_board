import React from 'react';
import LoginForm from "@/components/ui/LoginForm.tsx";

const LoginPage = () => {
    return (
        <>
            <LoginForm/>
        </>
    );
};

export default LoginPage;
